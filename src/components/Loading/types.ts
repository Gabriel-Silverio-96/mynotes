export interface LoadingProps {
    isLoading: boolean;
    justIcon?: boolean;
    alignCenter?: boolean;
}

export interface LoadingContainerProps {
    isLoading: boolean;
    alignCenter?: boolean;
}